import 'package:flutter/material.dart';

const kPrimaryColor = Color(0xff1E1E2E);
    const cBgDark = Color(0xFF0F1220);        // الخلفية الداكنة أعلى الشاشة
    const orange = Color(0xFFFF7A1A);        // البرتقالي للأزرار والروابط
    const grey = Color(0xFF9AA1A9);          // نص مساعد
    const white = Color(0xFFEFF4FA);       // خلفية الحقول
    const radiusBig = Radius.circular(32);
  // const black=Color.fromARGB(255, 13, 12, 12);
  //      color: Color.fromARGB(255, 34, 30, 30),
  //          color: Color(0xFF3A3F47),
